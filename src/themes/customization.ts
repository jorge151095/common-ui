export interface ICustomization {
    borderRadius: number;
    drawerWidth: number;
    fontFamily?: string;
    navType?: string;
}

const customization = {
    borderRadius: 12,
    drawerWidth: 260,
};

export { customization };
